import { CreateListClient } from "@/components/create-list-client";

export const metadata = {
  title: "Create a ranking — RankIt",
  description: "Put your order on the record."
};

export default function CreatePage() {
  return <CreateListClient />;
}
